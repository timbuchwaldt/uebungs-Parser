window.onload = function () {
    var i=0;
    <% @sum.each do |s| %>
    var <%= sanitize s.lecture + 'pie' %> = Raphael(10, i, 230, 230);
    <%= sanitize s.lecture+ 'pie' %>.g.piechart(110, 110, 100, [<%= s.possible %>,<%= s.result %>]);
    i = i+250;
    <% end %>

};